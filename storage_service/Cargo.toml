[package]
name = "storage_service"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] } # Async runtime
tonic = "0.8"                                  # gRPC support
prost = "0.11"                                 # Protocol Buffers
tigerbeetle = { git = "https://github.com/tigerbeetle/tigerbeetle", branch = "main" } # Tiger Beetle client
shared = { path = "../shared" }                # Link to shared crate

[build-dependencies]
tonic-build = "0.8"                            # For compiling .proto files
